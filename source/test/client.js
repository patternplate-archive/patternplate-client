import tape from 'blue-tape';

tape('Placeholder tests', async t => {
	t.comment('No actual tests yet');
	t.ok(true, 'true is true');

	t.test('some dummy tests', async t => {
		t.ok(true, 'true is true');
	});
});
